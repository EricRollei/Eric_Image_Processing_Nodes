[project]
name = "eric_image_processing_nodes"
description = "A comprehensive collection of advanced image processing nodes for ComfyUI, featuring state-of-the-art denoising, enhancement, and restoration techniques with GPU acceleration and specialized film grain processing."
version = "1.0.0"
license = {file = "LICENSE"} 
# classifiers = [
#     # For OS-independent nodes (works on all operating systems)
#     "Operating System :: OS Independent",
#
#     # OR for OS-specific nodes, specify the supported systems:
#     "Operating System :: Microsoft :: Windows",  # Windows specific
#     "Operating System :: POSIX :: Linux",  # Linux specific
#     "Operating System :: MacOS",  # macOS specific
#
#     # GPU Accelerator support. Pick the ones that are supported by your extension.
#     "Environment :: GPU :: NVIDIA CUDA",    # NVIDIA CUDA support
#     "Environment :: GPU :: AMD ROCm",       # AMD ROCm support
#     "Environment :: GPU :: Intel Arc",      # Intel Arc support
#     "Environment :: NPU :: Huawei Ascend",  # Huawei Ascend support
#     "Environment :: GPU :: Apple Metal",    # Apple Metal support
# ]

dependencies = ["# Eric's Image Processing Nodes - Requirements", "# Install with: pip install -r requirements.txt", "# Core dependencies", "numpy>=1.21.0", "opencv-python>=4.5.0", "scikit-image>=0.19.0", "scipy>=1.7.0", "PyWavelets>=1.3.0", "# Optional dependencies for enhanced functionality", "matplotlib>=3.3.0  # For visualization in test scripts", "torch>=1.9.0  # Already available in ComfyUI", "torchvision>=0.10.0  # For additional image processing utilities", "facexlib>=0.3.0  # Required for DiffBIR face restoration helpers", "# GPU acceleration (optional but recommended)", "# Install ONE of these based on your CUDA version:", "# cupy-cuda11x  # For CUDA 11.x", "# cupy-cuda12x  # For CUDA 12.x (recommended for latest systems)", "# Performance optimization (optional)", "# numba>=0.56.0  # For JIT compilation speedups", "# Development dependencies (optional)", "# pytest>=6.0.0  # For running tests", "# black>=21.0.0  # For code formatting", "# flake8>=3.9.0  # For linting", "# Note: For GPU acceleration, install CuPy manually:", "# pip install cupy-cuda12x  # or cupy-cuda11x for older CUDA versions", "# GPU acceleration provides 2-5x speedup for wavelet denoising on large images"]

[project.urls]
Repository = "https://github.com/EricRollei/Eric_Image_Processing_Nodes"
#  Used by Comfy Registry https://registry.comfy.org
Documentation = "https://github.com/EricRollei/Eric_Image_Processing_Nodes/wiki"
"Bug Tracker" = "https://github.com/EricRollei/Eric_Image_Processing_Nodes/issues"

[tool.comfy]
PublisherId = ""
DisplayName = "Eric_Image_Processing_Nodes"
Icon = ""
includes = [] 
# "requires-comfyui" = ">=1.0.0"  # ComfyUI version compatibility

